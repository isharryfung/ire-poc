CREATE TABLE verified_identities (
  id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  subject VARCHAR2(255) NOT NULL UNIQUE,
  username VARCHAR2(255) NOT NULL,
  active NUMBER(1) DEFAULT 1 NOT NULL
);
